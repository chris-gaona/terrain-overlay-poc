# Terrain Overlay POC

## Setup

1. Copy the example token file:
   cp token.example.js token.js

2. Add your Mapbox public token:
   https://account.mapbox.com/access-tokens/

3. Run locally or deploy via GitHub Pages

## Notes
- Do NOT commit `token.js`
- Use a restricted public token (URL + read scopes only)